<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tzcpa.mapper.teacher.ManageWhetherBeginMapper">
	<select id="getManageWhetherBegin" resultType="int">
		SELECT
			COUNT(*)
		FROM
			t_stu_answer
		WHERE
			class_id = #{classId}
	</select>
</mapper>